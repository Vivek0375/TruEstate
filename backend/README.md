Backend

1. Place the provided CSV dataset at `backend/data/sales.csv`.
2. Install dependencies and start the server:

   - Windows PowerShell:

```powershell
cd backend; npm install; npm start
```

3. API endpoint: `GET /api/sales` with query params for search, filters, sort, and pagination.

See code comments in `backend/src` for details.
